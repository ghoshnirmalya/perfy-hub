- args:
    name: user_projects
    table:
      name: user
      schema: public
    using:
      manual_configuration:
        column_mapping:
          projects: id
        remote_table:
          name: project
          schema: public
  type: create_array_relationship
