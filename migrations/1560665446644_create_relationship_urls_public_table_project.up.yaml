- args:
    name: pages
    table:
      name: site
      schema: public
    using:
      manual_configuration:
        column_mapping:
          id: site_id
        remote_table:
          name: page
          schema: public
  type: create_array_relationship
